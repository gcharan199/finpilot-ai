# Google Gemini API key — used by @finpilot/ai-engine's GeminiProvider.
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Gemini model id. gemini-2.5-flash is fast + multimodal (supports receipt images).
GEMINI_MODEL=gemini-2.5-flash
