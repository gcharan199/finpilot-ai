# FinPilot AI is 100% on-device — no cloud, no API key, no network.
# There are no environment variables to configure. This file is intentionally
# empty and kept only as a marker that nothing here reaches a server.
