# FinPilot AI runs 100% on-device — no cloud, no API key, no network.
# The mobile app needs no environment variables. AI chat / receipt OCR run via
# on-device native modules in an EAS dev build (see docs/on-device.md); all local
# finance features work with no configuration at all.
